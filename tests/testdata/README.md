Test-only localhost certificate material for HTTPS integration tests.

These files are intentionally checked in for deterministic test runs and are
only used under `tests/test_integration.cc`. They are not production secrets.
